"""Tools that live inside the Tower of Babel suite.

Each tool is a self-contained subpackage under ``tools/<name>/`` and
may only import from ``babel.*`` and ``shared.*``. Tools cannot
import from each other. See MASTER.md Section 4.3.
"""
